import React from 'react'
import logo from '../../images/logokobe.png';

const header = () => {
  return (
    <section className="lateral">
      <img src={logo} alt=' imagem lateral'/>
    </section>
  )
}

export default header
